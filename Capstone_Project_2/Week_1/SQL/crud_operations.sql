-- ==========================================
-- CRUD OPERATIONS
-- Retail Sales Analytics System
-- ==========================================

USE retail_sales_db;

-- ==========================================
-- CREATE
-- ==========================================

INSERT INTO sales
(product_id, store_id, quantity, sale_date, total_amount)
VALUES
(2, 1, 4, '2026-06-21', 10000);

-- ==========================================
-- READ
-- ==========================================

SELECT * FROM sales;

-- ==========================================
-- UPDATE
-- ==========================================

UPDATE sales
SET quantity = 6,
    total_amount = 15000
WHERE sale_id = 1;

-- ==========================================
-- DELETE
-- ==========================================

DELETE FROM sales
WHERE sale_id = 4;

-- ==========================================
-- VERIFY CRUD
-- ==========================================

SELECT * FROM sales;
